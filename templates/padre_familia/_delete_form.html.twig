<form method="post" action="{{ path('app_padre_familia_delete', {'id': padre_familium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ padre_familium.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
