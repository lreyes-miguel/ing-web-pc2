<form method="post" action="{{ path('app_oficinista_delete', {'id': oficinistum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oficinistum.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
