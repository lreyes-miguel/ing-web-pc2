{% extends 'base.html.twig' %}

{% block title %}Docente{% endblock %}

{% block body %}
    <h1>Docente</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ docente.id }}</td>
            </tr>
            <tr>
                <th>ApellidoPaterno</th>
                <td>{{ docente.apellidoPaterno }}</td>
            </tr>
            <tr>
                <th>ApellidoMaterno</th>
                <td>{{ docente.apellidoMaterno }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ docente.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ docente.email }}</td>
            </tr>
            <tr>
                <th>Celular</th>
                <td>{{ docente.celular }}</td>
            </tr>
            <tr>
                <th>Domicilio</th>
                <td>{{ docente.domicilio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_docente_index') }}" class="btn btn-secondary"><< Listar</a>

    <a href="{{ path('app_docente_edit', {'id': docente.id}) }}" class="btn btn-secondary">edit</a>

    {{ include('docente/_delete_form.html.twig') }}
{% endblock %}
